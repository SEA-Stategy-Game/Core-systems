extends StaticBody2D

var houseEntered = false
@onready var select = get_node("Selected")
var Selected = false


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
